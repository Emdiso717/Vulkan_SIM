// Expose a single Android application containing both the Jacobi XPBD and
// RID backends. The other example folders remain as implementation history
// but are deliberately not Gradle modules.
include ':xpbdvsrid3d'
project(':xpbdvsrid3d').projectDir = new File('examples/xpbdvsrid3d')
